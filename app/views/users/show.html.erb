<% if @user == current_user %>
  <h1>マイページ</h1>
<% else %>
  <h1><%= @user.name %>さんのページ</h1>
<% end %>
<ul>
  <li>名前：<%= @user.name %></li>
  <li>読み終わった書籍数：<%= @user.finished_books.count %></li>
</ul>
<%= link_to '戻る', :back, data: {turbolinks: false} %>
